import 'package:flutter/material.dart';

class SizesHelper {
  SizesHelper._();

  // Padding
  static const defaultPadding = EdgeInsets.all(24);

  // Spacing
  static const defaultSpace = SizedBox.square(dimension: 24);

  // Border Radius
  static const defaultBorderRadius = BorderRadius.all(Radius.circular(16));
}
